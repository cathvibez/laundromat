node_modules
dist
server-dist
.git
.gitignore
.DS_Store
*.local
__pycache__
tests
experiments
# NOT tools/ — `npm run build:server` in the build stage is
# `node tools/build-server.mjs`, so excluding it breaks the build with
# MODULE_NOT_FOUND. It never reaches the runtime image anyway: that stage
# copies only dist/ and server-dist/.
fly.toml
render.yaml
Dockerfile
.dockerignore
DEPLOY.md
